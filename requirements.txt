# Runtime dependencies for the FastAPI app + ML stack (pre-built wheels on Python 3.11–3.13, Windows).
# For Jupyter / full notebook stack, use: pip install -r requirements-dev.txt

fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
Jinja2==3.1.6
numpy==2.2.6
pandas==2.2.3
scipy==1.15.3
scikit-learn==1.6.1

# Tests (needed for `pytest`)
pytest>=8.3.0
httpx>=0.27.0
